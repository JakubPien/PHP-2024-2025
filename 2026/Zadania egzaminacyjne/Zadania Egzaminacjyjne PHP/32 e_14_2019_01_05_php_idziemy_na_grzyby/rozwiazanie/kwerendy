kw1:

SELECT nazwa, potoczna FROM grzyby WHERE jadalny = true;

kw2:

SELECT grzyby.nazwa, potoczna, rodzina.nazwa FROM grzyby JOIN rodzina ON grzyby.rodzina_id = rodzina.id JOIN potrawy ON grzyby.potrawy_id = potrawy.id WHERE potrawy.id = 1;

kw3:

SELECT nazwa_pliku, potoczna FROM grzyby

kw4:

ALTER TABLE grzyby ADD COLUMN opis VARCHAR(100);