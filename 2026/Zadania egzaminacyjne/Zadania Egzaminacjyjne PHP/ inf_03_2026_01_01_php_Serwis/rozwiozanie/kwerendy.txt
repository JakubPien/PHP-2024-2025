kw1:
SELECT marka, model, cena FROM pojazdy WHERE marka = "BM" ORDER BY cena DESC LIMIT 15;
kw2:
SELECT AVG(cena) AS "Średnia cena", MAX(cena) AS "Maksymalna cena" FROM pojazdy;
kw3:
SELECT marka, model cena, nazwa, doplata FROM pojazdy JOIN kolory ON kolory.id = pojazdy.kolor WHERE model = "alfa";
kw4:
SELECT marka, model, cena FROM pojazdy ORDER BY RAND() LIMIT 2;